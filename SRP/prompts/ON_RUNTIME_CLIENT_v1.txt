# ON_RUNTIME_CLIENT_v1

STATUS: DESIGN_PHASE
VERSION: v1
RUNTIME_CLIENT_VERSION: v1
COMPATIBILITY:
- runtime_executor_v1.py
- EXECUTOR_RUNTIME_v1
- EXECUTOR_SPEC_v1
- PARSER_RUNTIME_v1
- PARSER_v1_SPEC_FREEZE
- PARSER_OUTPUT_CONTRACT_v1_FREEZE
- RUNTIME_VALIDATION_BATCH_v1
- SRP_SCHEMA_v3

PURPOSE

Define the runtime LLM communication layer responsible for deterministic semantic parser execution.

This runtime client exists to:
- communicate with the LLM safely
- inject runtime instructions deterministically
- transmit raw runtime input
- receive runtime output
- preserve machine-safe execution behavior
- preserve runtime reproducibility
- preserve runtime authority integrity
- preserve deterministic orchestration

This runtime client MUST:
- remain transport-oriented
- preserve semantic neutrality
- preserve runtime authority boundaries
- preserve traceable execution behavior
- preserve deterministic invocation behavior

This runtime client MUST NOT:
- reinterpret semantic meaning
- mutate runtime outputs
- inject hidden prompts
- silently normalize responses
- fabricate continuity
- fabricate structure
- repair malformed outputs
- override runtime authority

The runtime client MUST obey:
- runtime_executor_v1.py
- EXECUTOR_RUNTIME_v1
- EXECUTOR_SPEC_v1
- PARSER_RUNTIME_v1
- PARSER_v1_SPEC_FREEZE
- PARSER_OUTPUT_CONTRACT_v1_FREEZE
- RUNTIME_VALIDATION_BATCH_v1
- SRP_SCHEMA_v3

Frozen artifacts remain authoritative.

---

# RUNTIME_CLIENT_PHILOSOPHY

The runtime client MUST behave as:
- deterministic transport layer
- runtime communication adapter
- execution-safe invocation layer

The runtime client MUST NOT behave as:
- semantic interpreter
- output repair layer
- continuity reasoning layer
- hidden orchestration layer

The runtime client MUST preserve:
- semantic neutrality
- invocation determinism
- runtime/output fidelity
- audit-safe communication traces

The runtime client MUST avoid:
- implicit prompt mutation
- hidden retries
- uncontrolled variance
- hidden preprocessing
- hidden postprocessing

---

# IMPLEMENTATION_LANGUAGE

Implementation language:
Python 3

The implementation SHOULD prioritize:
- readability
- traceability
- determinism
- modularity
- explicit execution behavior

The implementation MUST avoid:
- implicit mutation
- silent exceptions
- hidden fallback behavior
- uncontrolled retries
- hidden semantic normalization

---

# RUNTIME_INVOCATION_MODEL

The runtime client MUST:
1. receive runtime prompt
2. receive raw runtime input
3. construct deterministic runtime payload
4. invoke LLM provider safely
5. receive runtime response
6. return raw runtime response unchanged

The runtime client MUST preserve:
- raw runtime output fidelity
- runtime authority integrity
- deterministic invocation structure

The runtime client MUST NOT:
- mutate runtime response
- normalize runtime output
- inject semantic assumptions
- reinterpret unresolved states
- repair malformed structures

---

# PAYLOAD_CONSTRUCTION_RULES

The runtime client MUST construct:
- deterministic payload structure
- deterministic message ordering
- deterministic runtime boundaries

The runtime client MUST include:
- runtime instructions
- raw runtime input
- deterministic execution constraints

The runtime client MUST preserve:
- exact runtime instructions
- exact raw runtime input
- exact execution ordering

The runtime client MUST NOT:
- rewrite runtime prompts
- inject hidden instructions
- optimize prompts semantically
- reorder semantic instructions
- compress unresolved ambiguity

Payload construction MUST remain:
strict
predictable
and
audit-safe.

---

# MESSAGE_STRUCTURE_RULES

The runtime client SHOULD structure communication as:

SYSTEM:
runtime instructions

USER:
raw runtime input

The runtime client MUST preserve:
- runtime/system separation
- semantic boundary integrity
- deterministic role ordering

The runtime client MUST NOT:
- merge runtime and user layers
- inject assistant prefill
- inject hidden contextual memory
- inject historical executions

---

# LLM_PROVIDER_BEHAVIOR

The runtime client MUST:
- invoke a single runtime execution
- receive a single runtime response
- preserve isolated execution behavior

The runtime client MUST avoid:
- chained prompting
- recursive prompting
- hidden retry prompting
- semantic repair prompting
- hidden continuation prompting

The runtime client MUST preserve:
single-shot controlled execution.

---

# RESPONSE_HANDLING_RULES

The runtime client MUST:
- receive raw response safely
- preserve raw response exactly
- return raw response unchanged

The runtime client MUST NOT:
- repair malformed JSON
- remove prose automatically
- infer missing structures
- normalize malformed outputs
- reinterpret semantic meaning

Response handling MUST remain:
transport-only
and
non-interpretive.

---

# DETERMINISM_RULES

DETERMINISM_MODE:
LOW_VARIANCE_EXECUTION

The runtime client SHOULD minimize:
- response variance
- invocation variance
- payload variance

Equivalent runtime executions SHOULD produce:
- equivalent runtime outputs
- equivalent semantic structures
- equivalent continuity handling

The runtime client MUST avoid:
- probabilistic invocation mutation
- inconsistent runtime ordering
- hidden payload variability

---

# RETRY_RULES

RETRY_MODE:
NO_INTERNAL_RETRY

The runtime client MUST NOT:
- retry automatically
- repair failed responses
- reinterpret malformed responses
- re-prompt silently

Retry orchestration belongs to:
runtime_executor_v1.py

The runtime client MUST remain:
retry-neutral.

---

# ERROR_HANDLING_RULES

The runtime client MUST:
- expose invocation failures explicitly
- expose provider failures explicitly
- expose malformed responses explicitly

The runtime client MUST NOT:
- swallow exceptions silently
- hide provider instability
- normalize runtime failures
- fabricate fallback responses

Errors MUST remain:
- visible
- traceable
- audit-safe

---

# TRACEABILITY_RULES

The runtime client MUST preserve:
- invocation metadata
- request metadata
- response metadata
- provider metadata
- execution timestamps

The runtime client MUST preserve:
- reproducible invocation traces
- deterministic execution traces
- audit-safe communication traces

The runtime client MUST NOT:
- discard invocation failures
- discard malformed responses
- discard provider instability signals

---

# EXECUTION_ISOLATION_RULES

MEMORY_MODE:
STATELESS_FIRST

The runtime client MUST treat:
each invocation

as:
an isolated execution event.

The runtime client MUST NOT:
- inject hidden continuity
- inject hidden memory
- merge executions automatically
- preserve hidden conversational state

Longitudinal reconciliation remains:
OUTSIDE runtime client scope.

---

# PROVIDER_ABSTRACTION_RULES

The implementation SHOULD remain:
provider-agnostic.

The runtime client SHOULD support:
- interchangeable provider adapters
- isolated provider configuration
- deterministic provider boundaries

The runtime client MUST avoid:
- provider-coupled orchestration logic
- provider-specific semantic mutation
- provider-specific hidden normalization

---

# SECURITY_AND_SAFETY_RULES

The runtime client MUST:
- preserve runtime authority integrity
- preserve strict execution boundaries
- preserve deterministic transport behavior

The runtime client MUST avoid:
- hidden prompt injection
- semantic leakage
- runtime contamination
- execution-layer mutation

The runtime client MUST preserve:
transport-layer neutrality.

---

# FINAL_RUNTIME_CLIENT_RULE

The runtime client MUST prioritize:

safe deterministic transport of runtime semantic execution

over:
- convenience normalization
- semantic optimization
- hidden correction
- implicit repair
- provider-specific improvisation
- interpretive flexibility
- aggressive recovery