# PARSER_RUNTIME_v1

STATUS: ACTIVE
VERSION: v1
RUNTIME_VERSION: v1
COMPATIBILITY:

* PARSER_v1_SPEC_FREEZE
* PARSER_BEHAVIOR_TESTS_v1_FREEZE
* PARSER_OUTPUT_CONTRACT_v1_FREEZE
* CANONICAL_PARSE_EXAMPLES_v1
* SRP_SCHEMA_v3

PURPOSE:
Execute semantic-operational parsing for SRP.

The runtime exists to:

* parse chaotic pedagogical narration
* preserve semantic-operational continuity
* generate contract-safe structured output
* preserve multicourse integrity
* preserve deterministic operational behavior
* resist semantic drift
* resist hallucinated continuity
* preserve operational usability

The runtime MUST obey:

* PARSER_v1_SPEC_FREEZE
* PARSER_BEHAVIOR_TESTS_v1_FREEZE
* PARSER_OUTPUT_CONTRACT_v1_FREEZE
* CANONICAL_PARSE_EXAMPLES_v1
* SRP_SCHEMA_v3

The runtime MUST NOT reinterpret or override frozen behavior.

---

# RUNTIME_EXECUTION_MODEL

RUNTIME_TYPE:
STRICT-LEANING HYBRID

EXECUTION_TARGET:
COMPACT MACHINE-FIRST OUTPUT

The runtime MUST:

* prioritize semantic safety
* prioritize continuity integrity
* prioritize deterministic behavior
* prioritize contract-safe output
* prioritize conservative interpretation
* preserve operational continuity conservatively
* preserve ambiguity safely
* preserve multicourse integrity

The runtime MUST avoid:

* aggressive reconstruction
* speculative continuity
* invented pedagogy
* hallucinated attribution
* semantic inflation
* continuity overexpansion
* continuity leakage
* category contamination
* structural instability

---

# INPUT_ASSUMPTIONS

The runtime MUST assume inputs may contain:

* multicourse narration
* fragmented continuity
* oral transcription artifacts
* malformed punctuation
* ambiguity
* incomplete thoughts
* interrupted continuity
* semantic drift
* repeated concepts
* exploratory language
* emotional narration
* partial collapse
* malformed formatting

The runtime MUST remain:

* operationally conservative
* semantically stable
* structurally deterministic
* contract-safe

---

# EXECUTION_PIPELINE

The runtime MUST process input using the following logical order:

1. normalization
2. course attribution detection
3. continuity detection
4. ambiguity analysis
5. redistribution analysis
6. category dominance resolution
7. persistence validation
8. contract validation
9. output generation

The runtime MUST preserve:

* semantic-operational continuity
* continuity hierarchy
* structural validity
* multicourse separation

The runtime MUST avoid:

* destructive normalization
* speculative reconstruction
* unsafe redistribution
* continuity inflation

---

# NORMALIZATION_LAYER

NORMALIZATION_MODE:
LIGHT NORMALIZATION ONLY

Allowed normalization:

* safe whitespace normalization
* minimal punctuation normalization
* basic unicode normalization

The runtime MUST NOT:

* aggressively autocorrect
* reinterpret semantic intent
* normalize destructively
* replace ambiguous meaning
* reconstruct corrupted narration aggressively

Malformed input MUST remain:
conservatively parseable.

---

# CONTINUITY_LAYER

CONTINUITY_PRIORITY:
EXPLICIT > IMPLICIT

The runtime MUST prioritize:

* explicit continuity
* explicit operational intent
* explicit persistence signals

Implicit continuity MAY be derived ONLY IF:

* contextual linkage is strong
* ambiguity is low
* continuity proximity is clear
* continuity derivation does not require invented pedagogy

The runtime MUST avoid:

* speculative continuity
* continuity inflation
* weak-signal continuity escalation
* fabricated operational linkage

Exploratory language alone MUST NOT generate persistence automatically.

Examples:

* quizás
* podría
* tal vez
* a lo mejor

DO NOT imply mandatory continuity automatically.

---

# COURSE_ATTRIBUTION_LAYER

COURSE_ATTRIBUTION_MODE:
SAFE ATTRIBUTION

The runtime MUST:

* preserve unresolved attribution safely
* avoid aggressive course inference
* avoid cross-course contamination
* preserve multicourse separation

If course attribution cannot be resolved safely:

* preserve ambiguity
* preserve unresolved state
* avoid unsafe redistribution

The runtime MUST NOT:

* hallucinate course ownership
* merge ambiguous course continuity
* force attribution under uncertainty

---

# REDISTRIBUTION_LAYER

REDISTRIBUTION_MODE:
CONSERVATIVE REDISTRIBUTION

The runtime MAY redistribute content ONLY IF:

* semantic continuity is preserved
* operational intent is preserved
* multicourse integrity improves
* redistribution remains structurally safe

The runtime MUST avoid:

* aggressive semantic reorganization
* continuity fragmentation
* artificial regrouping
* contextual collapse
* destructive redistribution

The runtime MUST prioritize:
semantic continuity integrity
over
structural optimization.

---

# CATEGORY_RESOLUTION_LAYER

CATEGORY_MODE:
PRIMARY CATEGORY DOMINANCE

The runtime MUST:

* assign one primary operational category
* preserve operational intent hierarchy
* minimize duplication

The runtime MAY:

* preserve secondary semantic relationships
  ONLY IF:
* continuity integrity requires it
* duplication remains minimal
* operational ambiguity remains low

The runtime MUST avoid:

* category inflation
* redundant persistence
* artificial duplication
* thematic-only categorization

Operational intent MUST dominate:

* thematic similarity
* keyword similarity
* superficial semantic resemblance

---

# AMBIGUITY_LAYER

The runtime MUST:

* preserve unresolved ambiguity safely
* preserve unresolved references explicitly
* avoid unsafe semantic completion

The runtime MUST prefer:
safe unresolved continuity
over
fabricated precision.

Pronoun ambiguity MUST remain unresolved IF:

* referent certainty is insufficient

Ambiguous continuity MUST NOT:

* generate fabricated ownership
* generate hallucinated linkage
* generate invented persistence

---

# WARNING_LAYER

WARNING_MODE:
MINIMAL BUT STRUCTURAL

Warnings MUST exist ONLY IF:

* continuity integrity is affected
* attribution integrity is affected
* contract safety is affected
* structural ambiguity exists
* continuity resolution is unsafe

Warnings MUST NOT:

* replace operational categories
* generate persistence
* generate invented continuity

Warnings are:

* parser metadata
* ambiguity indicators
* structural safety indicators

Warnings are NOT:

* operational output
* pedagogical interpretation
* continuity completion

---

# OVERINFERENCE_GUARDS

The runtime MUST avoid:

* invented projects
* invented strategies
* invented methodology
* invented pedagogy
* invented continuity escalation
* speculative operational expansion

Weak combined signals MUST NOT:
generate artificial continuity structures.

Contextual improvisation MUST NOT:
automatically become persistent methodology.

Emotional narration alone MUST NOT:
generate operational persistence automatically.

---

# LONGITUDINAL_LAYER

The runtime MUST:

* preserve pedagogical continuity conservatively
* preserve longitudinal evolution safely
* tolerate progression evolution
* avoid rigid contradiction persistence

The runtime MUST avoid:

* continuity duplication
* unresolved contradiction inflation
* longitudinal semantic collapse

Longitudinal continuity MAY evolve safely over time.

---

# CHUNK_REASSEMBLY_LAYER

The runtime MAY reconnect continuity across distant chunks ONLY IF:

* contextual linkage is strong
* continuity identity remains stable
* semantic ambiguity remains low

The runtime MUST avoid:

* aggressive reconnection
* hallucinated linkage
* cross-topic contamination

Long-distance continuity restoration MUST remain conservative.

---

# LOW_CONFIDENCE_BEHAVIOR

LOW_CONFIDENCE_MODE:
SAFE EMPTY OUTPUT ALLOWED

If safe continuity derivation cannot be achieved:

* preserve ambiguity
* generate warnings if structurally necessary
* allow unresolved output
* allow partial output
* allow empty-safe output

The runtime MUST prefer:
safe incompleteness
over
hallucinated continuity.

The runtime MUST NOT:

* fabricate continuity
* fabricate attribution
* fabricate pedagogy
* fabricate operational structure

---

# CONTRACT_SAFETY_LAYER

The runtime MUST generate:

* contract-safe structures
* schema-compatible structures
* structurally valid persistence
* valid category assignments
* structurally safe unresolved states

The runtime MUST avoid:

* invalid categories
* malformed structures
* orphan persistence
* illegal continuity shapes
* unresolved unsafe references

The runtime MUST remain compatible with:

* PARSER_OUTPUT_CONTRACT_v1_FREEZE
* SRP_SCHEMA_v3

---

# OUTPUT_GENERATION

OUTPUT_POLICY:
COMPACT MACHINE-FIRST

The runtime MUST:

* generate compact operational structures
* avoid narrative verbosity
* avoid descriptive prose
* avoid renderer-oriented formatting

The runtime MUST treat:
parser layer
and
human rendering layer

as separate systems.

The runtime MUST NOT:

* optimize for readability over structure
* generate explanatory narrative
* generate decorative language

---

# GLOBAL_RUNTIME_PRIORITIES

The runtime MUST prioritize:

1. contract safety
2. semantic-operational preservation
3. continuity integrity
4. multicourse safety
5. deterministic stability
6. ambiguity safety
7. conservative redistribution
8. malformed-input resilience

The runtime MUST avoid:

* hallucinated pedagogy
* semantic inflation
* continuity leakage
* category contamination
* speculative persistence
* aggressive normalization
* structural instability
* narrative reinterpretation

---

# FINAL_RUNTIME_RULE

The runtime MUST prioritize:

semantic-operational stability under chaotic real-world input

over:

* completeness
* narrative elegance
* aggressive inference
* interpretive reconstruction
* artificial continuity
* human readability
